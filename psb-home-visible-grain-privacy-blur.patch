--- a/src/features/bank/bank.module.css
+++ b/src/features/bank/bank.module.css
@@ -1085,8 +1085,8 @@
 .sensitiveHidden {
   position: relative;
   overflow: visible;
-  filter: blur(5px) saturate(1.08);
-  opacity: .72;
+  filter: blur(4.6px) saturate(1.1) contrast(1.08);
+  opacity: .76;
   pointer-events: none;
   user-select: none;
   transition: filter 220ms ease, opacity 220ms ease;
@@ -1095,25 +1095,38 @@
 .sensitiveHidden::after {
   position: absolute;
   z-index: 1;
-  inset: -7px -10px;
+  inset: -9px -12px;
   background-image:
-    radial-gradient(circle, rgb(255 255 255 / 76%) 0 .9px, transparent 1.8px),
-    radial-gradient(circle, rgb(255 255 255 / 42%) 0 1.2px, transparent 2.4px),
-    radial-gradient(circle, rgb(155 165 184 / 42%) 0 .8px, transparent 1.8px);
-  background-position: 1px 2px, 8px 13px, 14px 6px;
-  background-size: 13px 15px, 19px 17px, 23px 21px;
+    radial-gradient(circle, rgb(255 255 255 / 92%) 0 1px, transparent 1.25px),
+    radial-gradient(circle, rgb(255 255 255 / 68%) 0 1.15px, transparent 1.45px),
+    radial-gradient(circle, rgb(167 177 196 / 72%) 0 .95px, transparent 1.2px),
+    radial-gradient(circle, rgb(255 255 255 / 48%) 0 .7px, transparent 1px);
+  background-position: 0 1px, 4px 6px, 8px 2px, 2px 9px;
+  background-size: 7px 8px, 9px 10px, 11px 9px, 6px 11px;
   content: "";
-  filter: blur(.8px);
   mix-blend-mode: screen;
-  opacity: .72;
+  opacity: .9;
   pointer-events: none;
-  animation: sensitive-particles 3.4s ease-in-out infinite alternate;
+  mask-image: radial-gradient(ellipse at center, #000 52%, rgb(0 0 0 / 86%) 70%, transparent 100%);
+  animation: sensitive-particles 2.8s steps(6, end) infinite alternate;
 }
 
 @keyframes sensitive-particles {
-  0% { transform: translate3d(-1.5px, -1px, 0); opacity: .58; }
-  50% { transform: translate3d(1px, 1.5px, 0); opacity: .78; }
-  100% { transform: translate3d(2px, -.5px, 0); opacity: .66; }
+  0% {
+    transform: translate3d(-1px, -1px, 0);
+    background-position: 0 1px, 4px 6px, 8px 2px, 2px 9px;
+    opacity: .78;
+  }
+  50% {
+    transform: translate3d(1px, 0, 0);
+    background-position: 2px 0, 3px 8px, 10px 3px, 1px 7px;
+    opacity: .96;
+  }
+  100% {
+    transform: translate3d(0, 1px, 0);
+    background-position: 1px 3px, 6px 5px, 7px 0, 4px 10px;
+    opacity: .84;
+  }
 }
 
 @media (prefers-reduced-motion: reduce) {
@@ -1135,8 +1148,8 @@
 .sensitiveHistory .transactionAmount {
   position: relative;
   overflow: visible;
-  filter: blur(5px) saturate(1.12);
-  opacity: .68;
+  filter: blur(4.6px) saturate(1.14) contrast(1.08);
+  opacity: .72;
   user-select: none;
   transition: filter 220ms ease, opacity 220ms ease;
 }
@@ -1147,19 +1160,20 @@
 .sensitiveHistory .transactionAmount::after {
   position: absolute;
   z-index: 2;
-  inset: -6px -8px;
+  inset: -8px -10px;
   background-image:
-    radial-gradient(circle, rgb(255 255 255 / 72%) 0 .85px, transparent 1.7px),
-    radial-gradient(circle, rgb(255 255 255 / 38%) 0 1.1px, transparent 2.2px),
-    radial-gradient(circle, rgb(151 161 181 / 44%) 0 .75px, transparent 1.6px);
-  background-position: 0 1px, 7px 9px, 12px 4px;
-  background-size: 12px 14px, 17px 19px, 21px 16px;
+    radial-gradient(circle, rgb(255 255 255 / 94%) 0 1px, transparent 1.25px),
+    radial-gradient(circle, rgb(255 255 255 / 70%) 0 1.1px, transparent 1.4px),
+    radial-gradient(circle, rgb(160 171 192 / 74%) 0 .9px, transparent 1.2px),
+    radial-gradient(circle, rgb(255 255 255 / 50%) 0 .7px, transparent 1px);
+  background-position: 0 1px, 4px 6px, 8px 2px, 2px 9px;
+  background-size: 7px 8px, 9px 10px, 11px 9px, 6px 11px;
   content: "";
-  filter: blur(.7px);
   mix-blend-mode: screen;
-  opacity: .7;
+  opacity: .9;
   pointer-events: none;
-  animation: sensitive-particles 3.1s ease-in-out infinite alternate;
+  mask-image: radial-gradient(ellipse at center, #000 56%, rgb(0 0 0 / 86%) 74%, transparent 100%);
+  animation: sensitive-particles 2.8s steps(6, end) infinite alternate;
 }
 
 .sensitiveHistory .transactionMain,
--- a/AGENT_READ_ME.md
+++ b/AGENT_READ_ME.md
@@ -20,7 +20,7 @@
 
 - Participant pages keep exactly 24 px of scroll-end spacing after the final content block when no tabbar is present; when the tabbar is present, reserve the tabbar's full height plus the same 24 px gap. Route-local wrappers must not add a second bottom gap.
 - The bottom “Перейти к обмену” button in the home currency-exchange block uses a 16 px squircle/continuous-corner shape instead of a pill. Preserve `home.exchange.open` and its existing unavailable-demo behavior.
-- Home-screen privacy masking uses a soft granular/particle blur rather than a flat Gaussian smear. The particles drift subtly, the underlying colored merchant icons still bleed through, and the blur bloom must be allowed to extend beyond the text/icon boxes instead of being clipped by content wrappers.
+- Home-screen privacy masking uses a visibly granular particle/noise blur rather than a flat Gaussian smear. Fine bright grain remains clearly readable on-device while drifting subtly, the underlying colored merchant icons still bleed through, and the blur bloom must extend beyond the text/icon boxes instead of being clipped by content wrappers.
 
 ## Guardrails
 
